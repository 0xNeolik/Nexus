<div class="container">

  <div class="row">
    <div class="col-md-6">
      <form action="/auth/{{user.id}}/edit" method="POST" enctype="multipart/form-data">

        <div class="form-group">
          <label>Nueva imagen
            <input type="file" name="new-image" class="form-control-file" />
            <input type="text" name="existingImage" hidden value="{{user.image}}" />
          </label>
        </div>

        <div class="form-group">
          <label for="name">Nuevo nombre</label>
          <input type="text" class="form-control" id="name" name="name" value="{{user.name}}" required />
        </div>

        <div class="form-group">
          <label for="password">Nueva descripcion</label>
          <input
            type="text"
            class="form-control"
            id="password"
            name="description"
            value="{{user.description}}"
            required
          />
        </div>

        <button type="submit" class="btn btn-dark btn-block">Save</button>
      </form>
    </div>
  </div>
</div>